import React from 'react';
import { View } from 'react-native';

// File dummy untuk mencegah error "No route named 'action' exists".
// Tab 'action' di _layout.tsx digunakan sebagai spacer untuk tombol kamera di tengah.
export default function ActionScreen() {
  return <View />;
}
